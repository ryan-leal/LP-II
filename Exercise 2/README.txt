Para compilar e executar o programa em sistemas que possuem suporte para a biblioteca pthread.h:
	-Abra o Terminal no diretório onde se encontra o arquivo main.c
	-Digite no terminal o comando entre aspas: "gcc main.c -pthread -o main"
	-Então execute usando: "./main"
OBS: Na pasta existem também prints contendo a execução.
